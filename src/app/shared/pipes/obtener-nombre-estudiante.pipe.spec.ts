import { ObtenerNombreEstudiantePipe } from './obtener-nombre-estudiante.pipe';

describe('ObtenerNombreEstudiantePipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerNombreEstudiantePipe();
    expect(pipe).toBeTruthy();
  });
});
