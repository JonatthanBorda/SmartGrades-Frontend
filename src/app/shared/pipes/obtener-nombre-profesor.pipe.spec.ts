import { ObtenerNombreProfesorPipe } from './obtener-nombre-profesor.pipe';

describe('ObtenerNombreProfesorPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerNombreProfesorPipe();
    expect(pipe).toBeTruthy();
  });
});
